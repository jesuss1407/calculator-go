name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend:
    name: Backend (Go)
    runs-on: ubuntu-latest
    defaults:
      run:
        # An explicit bash shell adds -o pipefail, so a failing command in a pipe fails the step.
        shell: bash
        working-directory: backend
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: backend/go.mod
          # Standard library only: there is no go.sum for the module cache to key on.
          cache: false

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-formatted:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Test with coverage
        run: go test ./... -coverprofile coverage.out

      - name: Coverage summary
        run: |
          go tool cover -func coverage.out
          {
            echo '### Backend coverage'
            echo '```'
            go tool cover -func coverage.out
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  frontend:
    name: Frontend (React)
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test with coverage
        run: npm run coverage | tee coverage.txt

      - name: Coverage summary
        run: |
          {
            echo '### Frontend coverage'
            echo '```'
            sed -n '/% Coverage report/,$p' coverage.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Type-check and build
        run: npm run build

  docker:
    name: Docker image and E2E
    runs-on: ubuntu-latest
    needs: [backend, frontend]

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v7

      - name: Build image
        run: docker build -t calculator .

      # The container keeps running for the smoke and E2E steps. No --rm, so its logs
      # survive a crash; the runner is discarded after the job anyway.
      - name: Start container
        run: |
          docker run -d --name calculator -p 8080:8080 calculator

          # Wait for the server to become ready.
          curl -sf \
            --retry 10 \
            --retry-connrefused \
            --retry-delay 1 \
            http://localhost:8080/health
          echo

      # Quick curl checks fail fast, before a minute is spent installing browsers.
      - name: Smoke test the container
        run: |
          # Verify that the built frontend is being served.
          page=$(curl -sf http://localhost:8080/)
          grep -q '<title>Calculator</title>' <<< "$page"
          echo "page: served"

          # Verify one end-to-end API calculation.
          result=$(curl -sf \
            -X POST http://localhost:8080/api/v1/calculate \
            -H 'Content-Type: application/json' \
            -d '{"operation":"add","a":2,"b":3}')

          echo "calculate: $result"
          test "$result" = '{"result":5}'

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install Playwright
        working-directory: frontend
        run: |
          npm ci
          npx playwright install --with-deps chromium

      # Runs against the container started above (the default BASE_URL, port 8080).
      - name: E2E tests
        working-directory: frontend
        run: npm run e2e

      - name: Container logs
        if: always()
        run: docker logs calculator || true

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            frontend/playwright-report/
            frontend/test-results/
          retention-days: 7
